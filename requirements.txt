# ARCANE - Neuromimetic Semantic Foundation Model
# Core library dependencies

# Machine Learning & Neural Networks
tensorflow>=2.12.0,<3.0.0
numpy>=1.21.0,<3.0.0
keras>=2.12.0
# pandas (pulled in by Keras) imports numexpr; wheels older than 2.10 crash on NumPy 2
# with AttributeError: _ARRAY_API not found
numexpr>=2.10.2

# Data Processing & Visualization
matplotlib>=3.5.0

# HTTP client (for downloading datasets)
requests>=2.28.0

# MNIST demo API server (serve_mnist_api.py, Render)
fastapi>=0.100.0
uvicorn>=0.22.0
Pillow>=9.0.0

# Development dependencies (optional)
# pip install gpbacay-arcane[dev]
# pytest>=7.0.0
# black>=23.0.0
# flake8>=6.0.0

# python setup.py sdist bdist_wheel
# pip install dist/gpbacay_arcane-3.0.2-py3-none-any.whl --force-reinstall
# twine upload dist/*

